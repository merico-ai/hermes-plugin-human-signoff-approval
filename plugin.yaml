name: human-signoff-approval
version: 0.1.0
description: "Handles APPROVAL_PENDING responses from Human Signoff. Injects instructions so the agent presents the approval URL to the user, waits for approval via `signoff wait`, then retries the blocked action."
author: "Human Signoff Project"
hooks:
  - pre_llm_call
