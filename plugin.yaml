name: human-signoff-approval
version: 0.1.0
description: "Handles APPROVAL_PENDING responses from the human-signoff proxy. Injects instructions so the agent presents the approval URL to the user and stops, without polling or retrying the blocked command."
author: "Human Signoff Project"
hooks:
  - pre_llm_call
